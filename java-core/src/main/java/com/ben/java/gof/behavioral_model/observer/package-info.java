package com.ben.java.gof.behavioral_model.observer;

/**
 * 观察者模式:
 *
 * 1.定义
 *      定义一种一对多的依赖关系, 一个主题(目标)对象可被多个观察者对象同时监听,使得每当主题对象状态变化时,
 *      所有依赖于它的对象都会得到通知并被自动更新;
 *
 * 2.使用场景和注意事项
 *   2.1 如果观察者数量过多,则事件通知会耗时较长(多线程缓解)
 *   2.2 事件通知呈线性关系,如果其中一个观察者处理事件卡壳,会影响后续的观察者接收事件(多线程缓解)
 *   2.3 如果观察者和被观察者之间存在循环依赖,则可能造成两者之间的循环调用,导致系统崩溃;
 * 3.源码分析
 *  jdk: ServletContextListener
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 */